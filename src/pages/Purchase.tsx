import React from 'react';
import { Text } from 'react-native';
import { View } from 'react-native';

export function Purchase(){
   return (
      <View>
         <Text>Prato Pedido com Sucesso!</Text>
      </View>
   );
}